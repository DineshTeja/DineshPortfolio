/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['media.licdn.com', 'thesmartcube.com','www.thesmartcube.com', 'upload.wikimedia.org'],
  }
}

module.exports = nextConfig
